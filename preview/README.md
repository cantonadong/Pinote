# Stick UI Preview

双击 `index.html` 即可运行；也可以在仓库根目录执行：

```powershell
Start-Process .\preview\index.html
```

控制面板可切换 normal、loading、empty、error、edge case 与 Light/Dark。Todo 可点击切换；每个内容块均可向左或向右拖动删除，删除后可在 2 秒内撤销。“重播删除演示”用于重复观察动效。

这是独立 mock Preview，不包含数据库、通知、权限或其他真实业务逻辑。
